{
  "name": "lingo-mobile",
  "version": "1.54.1",
  "private": true,
  "description": "Çevrimdışı Türkçe Lingo — Android ve mobil web",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node --check server.js && node --check app.js && node --check mobile/app.js && node --check mobile/engine.js && node --check words.js",
    "lint": "node --check server.js && node --check app.js && node --check mobile/app.js && node --check mobile/engine.js && node --check words.js",
    "test": "node --test tests/engine.test.cjs",
    "test:ui": "node tests/ui.test.cjs",
    "android:build": "cd android && ./gradlew assembleDebug bundleRelease lintRelease"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1"
  },
  "displayVersion": "1.54.01"
}
